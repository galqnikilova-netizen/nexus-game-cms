<x-layouts.app title="{{ __('ui.auth.login') }} · NEXUS"><section class="auth-stage"><div class="auth-visual"><div class="eyebrow"><i></i> {{ __('ui.auth.eyebrow') }}</div><h1>{{ __('ui.auth.title') }}<br><span>{{ __('ui.auth.accent') }}</span></h1><p>{{ __('ui.auth.copy') }}</p></div><div class="auth-card"><div class="brand"><span class="brand-mark">✣</span><span><b>NEXUS</b><small>CONTROL ACCESS</small></span></div><h2>{{ __('ui.auth.login') }}</h2><p>{{ __('ui.auth.hint') }}</p><form method="POST" action="{{ route('login') }}">@csrf<label>{{ __('ui.auth.email') }}<input type="email" name="email" value="{{ old('email') }}" required autofocus autocomplete="email"></label>@error('email')<span class="form-error">{{ $message }}</span>@enderror<label>{{ __('ui.auth.password') }}<input type="password" name="password" required autocomplete="current-password"></label><label class="check"><input type="checkbox" name="remember" value="1"><span>{{ __('ui.auth.remember') }}</span></label><button class="btn btn-primary" type="submit">{{ __('ui.auth.submit') }} <b>→</b></button></form></div></section></x-layouts.app>

