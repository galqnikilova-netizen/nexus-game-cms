<x-layouts.app title="NEXUS Game CMS"><section class="hero"><div class="shell hero-grid"><div><div class="eyebrow"><i></i> {{ __('ui.hero.eyebrow') }}</div><h1>{{ __('ui.hero.title') }}<br><span>{{ __('ui.hero.accent') }}</span></h1><p class="hero-copy">{{ __('ui.hero.copy') }}</p><div class="hero-actions"><a class="btn btn-primary" href="{{ route('servers.index') }}">{{ __('ui.hero.servers') }} <b>↗</b></a><a class="btn btn-quiet" href="{{ route('login') }}">{{ __('ui.hero.control') }}</a></div><div class="trust-row"><span><b>99.9%</b> uptime ready</span><span><b>BG/EN</b> localization</span><span><b>24/7</b> monitoring</span></div></div><div class="hero-console"><div class="console-top"><span><i></i> LIVE NETWORK</span><small>QUERY ENGINE</small></div><div class="pulse-orbit"><div class="pulse-core">N</div><i></i><i></i><i></i></div><div class="console-metrics"><span><small>ONLINE</small><b>{{ $servers->where('status','online')->count() }}</b></span><span><small>PLAYERS</small><b>{{ $servers->sum('players_online') }}</b></span><span><small>SERVERS</small><b>{{ $servers->count() }}</b></span></div></div></div></section><section class="network shell"><div class="section-head"><div><div class="eyebrow"><i></i> {{ __('ui.network.eyebrow') }}</div><h2>{{ __('ui.network.title') }}</h2></div><a href="{{ route('servers.index') }}">{{ __('ui.network.all') }} →</a></div><div class="server-grid">@forelse($servers as $server)<article class="server-card"><div class="server-icon">{{ strtoupper(substr($server->game,0,2)) }}</div><div class="server-main"><small>{{ strtoupper($server->game) }}</small><h3>{{ $server->name }}</h3><p>{{ $server->host }}:{{ $server->port }}</p></div><div class="server-live"><span class="status {{ $server->status }}">{{ $server->status }}</span><b>{{ $server->players_online }}<small>/{{ $server->players_max }}</small></b></div></article>@empty<article class="empty-state"><span>⌁</span><h3>{{ __('ui.network.empty_title') }}</h3><p>{{ __('ui.network.empty_copy') }}</p></article>@endforelse</div></section></x-layouts.app>

