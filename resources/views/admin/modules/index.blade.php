<x-layouts.admin title="Modules · NEXUS" heading="Modules"><div class="grid gap-4 md:grid-cols-2">@forelse($modules as $module)<article class="n3-panel flex gap-4 p-6"><span class="grid h-12 w-12 shrink-0 place-items-center rounded-xl bg-white/6 text-xl text-[var(--accent)]">◇</span><div><small class="n3-eyebrow">{{ $module->slug }} · v{{ $module->version }}</small><h2 class="mt-2 text-lg font-black">{{ $module->name }}</h2><p class="mt-2 text-xs leading-5 text-slate-500">{{ $module->description }}</p><form class="mt-4" method="POST" action="{{ route('admin.modules.toggle',$module) }}">@csrf @method('PATCH')<button class="n3-button ghost">{{ $module->enabled?'Disable':'Enable' }}</button></form></div></article>@empty<div class="n3-panel p-12 text-center text-slate-500">No modules found.</div>@endforelse</div></x-layouts.admin>
