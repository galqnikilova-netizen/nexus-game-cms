<x-layouts.admin title="Modules · NEXUS" heading="Modules"><div class="grid gap-4 md:grid-cols-2">@forelse($modules as $module)<article class="nx-card flex gap-4 p-6"><span class="grid h-12 w-12 shrink-0 place-items-center rounded-xl bg-white/6 text-xl text-[var(--accent)]">◇</span><div><small class="nx-kicker">{{ $module->slug }} · v{{ $module->version }}</small><h2 class="mt-2 text-lg font-black">{{ $module->name }}</h2><p class="mt-2 text-xs leading-5 text-slate-500">{{ $module->description }}</p><form class="mt-4" method="POST" action="{{ route('admin.modules.toggle',$module) }}">@csrf @method('PATCH')<button class="nx-button-muted">{{ $module->enabled?'Disable':'Enable' }}</button></form></div></article>@empty<div class="nx-card p-12 text-center text-slate-500">No modules found.</div>@endforelse</div></x-layouts.admin>
