<x-layouts.admin title="Сървъри · NEXUS" heading="Управление на сървъри"><section class="admin-panel"><div class="panel-head"><div><small>INFRASTRUCTURE</small><h2>Всички сървъри</h2></div><a class="btn btn-primary" href="{{ route('admin.servers.create') }}">+ Нов сървър</a></div><div class="admin-table">@foreach($servers as $server)<div class="table-row"><span class="dot {{ $server->status }}"></span><div><b>{{ $server->name }}</b><small>{{ $server->host }}:{{ $server->port }}</small></div><span>{{ strtoupper($server->game) }}</span><span>{{ $server->is_visible?'Публичен':'Скрит' }}</span><div class="row-actions"><a href="{{ route('admin.servers.edit',$server) }}">Редакция</a><form method="POST" action="{{ route('admin.servers.destroy',$server) }}" onsubmit="return confirm('Да премахна ли сървъра?')">@csrf @method('DELETE')<button>Изтрий</button></form></div></div>@endforeach</div>{{ $servers->links() }}</section></x-layouts.admin>

