<x-layouts.admin title="Roles · NEXUS" heading="Roles & permissions"><div class="flex justify-end"><a class="nx-button" href="{{ route('admin.roles.create') }}">+ New role</a></div><div class="mt-4 grid gap-4 md:grid-cols-2">@foreach($roles as $role)<article class="nx-card p-6"><small class="nx-kicker">{{ $role->slug }}</small><h2 class="mt-3 text-xl font-black">{{ $role->name }}</h2><p class="mt-2 text-xs text-slate-500">{{ count($role->permissions) }} permissions · {{ $role->users_count }} users</p>@unless($role->is_system)<a class="mt-5 inline-block text-xs text-[var(--accent)]" href="{{ route('admin.roles.edit',$role) }}">Edit role →</a>@endunless</article>@endforeach</div></x-layouts.admin>
