<x-layouts.admin title="Роля · NEXUS" heading="{{ $role->exists?'Редакция на роля':'Нова роля' }}"><form class="admin-form" method="POST" action="{{ $role->exists?route('admin.roles.update',$role):route('admin.roles.store') }}">@csrf @if($role->exists)@method('PUT')@endif<div class="form-section"><div><small>PERMISSIONS</small><h2>Достъп</h2><p>Дай само необходимите права.</p></div><div class="form-grid"><label class="wide">Име<input name="name" value="{{ old('name',$role->name) }}" required></label><div class="permission-grid wide">@foreach($available as $permission)<label><input type="checkbox" name="permissions[]" value="{{ $permission }}" @checked(in_array($permission,old('permissions',$role->permissions??[]),true))><span>{{ $permission }}</span></label>@endforeach</div></div></div><div class="form-actions"><a class="btn btn-quiet" href="{{ route('admin.roles.index') }}">Назад</a><button class="btn btn-primary">Запази ролята</button></div></form></x-layouts.admin>
