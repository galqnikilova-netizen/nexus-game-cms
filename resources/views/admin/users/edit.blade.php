<x-layouts.admin title="{{ $member->name }} · NEXUS" heading="Управление на профил"><form class="admin-form" method="POST" action="{{ route('admin.users.update',$member) }}">@csrf @method('PUT')<div class="form-section"><div><small>USER IDENTITY</small><h2>{{ $member->name }}</h2><p>{{ $member->email }}</p></div><div class="form-grid"><label>Име<input name="name" value="{{ old('name',$member->name) }}" required></label><label>Език<select name="locale"><option value="bg" @selected($member->locale==='bg')>Български</option><option value="en" @selected($member->locale==='en')>English</option></select></label><label>Роля<select name="role_id"><option value="">Без допълнителна роля</option>@foreach($roles as $role)<option value="{{ $role->id }}" @selected($member->role_id===$role->id)>{{ $role->name }}</option>@endforeach</select></label><label>Статус<select name="status"><option value="active">Активен</option><option value="suspended">Временно спрян</option><option value="banned">Блокиран</option></select></label></div></div><div class="form-actions"><a class="btn btn-quiet" href="{{ route('admin.users.index') }}">Назад</a><button class="btn btn-primary">Запази</button></div></form></x-layouts.admin>
