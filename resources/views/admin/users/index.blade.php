<x-layouts.admin title="Потребители · NEXUS" heading="Потребители"><section class="admin-panel"><div class="panel-head"><div><small>IDENTITY SYSTEM</small><h2>Всички профили</h2></div><a href="{{ route('admin.roles.index') }}" class="btn btn-quiet">Роли и права</a></div><div class="admin-table">@foreach($users as $member)<div class="table-row user-row"><span class="user-avatar">{{ strtoupper(substr($member->name,0,1)) }}</span><div><b>{{ $member->name }}</b><small>{{ $member->email }}</small></div><span>{{ $member->nexusRole?->name ?? strtoupper($member->role) }}</span><span class="status {{ $member->status==='active'?'online':'offline' }}">{{ $member->status }}</span><a href="{{ route('admin.users.edit',$member) }}">Управление →</a></div>@endforeach</div>{{ $users->links() }}</section></x-layouts.admin>
