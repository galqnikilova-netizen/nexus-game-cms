<x-layouts.app title="{{ app()->getLocale()==='bg'?'Новини':'News' }} · NEXUS"><section class="n3-page-hero"><div class="n3-wrap"><div class="n3-breadcrumb"><a href="{{ route('home') }}">Home</a><span>/</span><b>News</b></div><span class="n3-eyebrow">Community journal</span><h1>{{ app()->getLocale()==='bg'?'Последни новини':'Latest news' }}</h1><p>{{ app()->getLocale()==='bg'?'Обновления, събития и новини от NEXUS мрежата.':'Updates, events and news from the NEXUS network.' }}</p></div></section><div class="n3-content"><div class="n3-wrap"><div class="n3-heading"><div><span class="n3-eyebrow">News archive</span><h2>{{ app()->getLocale()==='bg'?'Всички публикации':'All publications' }}</h2></div><strong class="text-[9px] text-slate-500">{{ $posts->total() }} ARTICLES</strong></div><section class="n3-news-grid">@forelse($posts as $post)<article class="n3-news-card"><a class="n3-news-card-image" href="{{ route('news.show',$post) }}" @if($post->imageUrl()) style="background-image:url('{{ $post->imageUrl() }}')" @endif></a><div class="n3-news-card-body"><small class="n3-eyebrow">{{ $post->category }} · {{ $post->published_at->format('d.m.Y') }}</small><h2><a href="{{ route('news.show',$post) }}">{{ $post->title }}</a></h2><p>{{ $post->excerpt }}</p><a class="n3-text-link" href="{{ route('news.show',$post) }}">{{ app()->getLocale()==='bg'?'Прочети новината':'Read article' }} →</a></div></article>@empty<div class="n3-panel col-span-full p-14 text-center text-xs text-slate-500">No news published.</div>@endforelse</section><div class="mt-6">{{ $posts->links() }}</div></div></div></x-layouts.app>
