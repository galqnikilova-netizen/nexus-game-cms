<x-layouts.app title="Новини · NEXUS">
    <section class="portal-subhead"><div class="portal-shell"><div class="portal-breadcrumb"><a href="{{ route('home') }}">HOME</a><span>/</span><b>NEWS</b></div><small>COMMUNITY JOURNAL</small><h1>LATEST NEWS</h1><p>Обновления, събития и новини от NEXUS мрежата.</p></div></section>
    <div class="portal-shell portal-page-grid"><x-portal-sidebar /><main class="portal-page-main"><header class="portal-heading"><div><small>NEWS ARCHIVE</small><h2>ALL PUBLICATIONS</h2></div><b>{{ $posts->total() }} ARTICLES</b></header><div class="news-card-grid">@forelse($posts as $post)<article><a class="news-card-image" href="{{ route('news.show',$post) }}" @if($post->imageUrl()) style="--news-image:url('{{ $post->imageUrl() }}')" @endif><span>{{ strtoupper($post->category) }}</span></a><div><small>{{ $post->published_at->format('d.m.Y') }} · {{ $post->author?->name ?? 'NEXUS TEAM' }}</small><h2><a href="{{ route('news.show',$post) }}">{{ $post->title }}</a></h2><p>{{ $post->excerpt }}</p><a href="{{ route('news.show',$post) }}">READ ARTICLE →</a></div></article>@empty<div class="portal-empty"><b>NO NEWS PUBLISHED</b><p>Първата публикация ще се появи тук.</p></div>@endforelse</div>{{ $posts->links() }}</main></div>
</x-layouts.app>
