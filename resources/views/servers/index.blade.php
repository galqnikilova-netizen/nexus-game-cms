<x-layouts.app title="Сървъри · NEXUS"><section class="inner-hero server-browser-hero"><div class="shell"><div class="eyebrow"><i></i> LIVE SERVER NETWORK</div><h1>Намери следващия<br><span>си мач.</span></h1><p>CS2, CS 1.6 и Minecraft общности — live статус, карти и играчи на едно място.</p><div class="browser-pills"><span><i></i> LIVE QUERY</span><span>{{ $servers->total() }} SERVERS</span><span>{{ $servers->sum('players_online') }} PLAYERS</span></div></div></section><section class="shell page-body"><div class="section-head"><div><div class="eyebrow"><i></i> SERVER BROWSER</div><h2>Всички сървъри</h2></div><small class="query-state">AUTO REFRESH · 60S</small></div><div class="server-grid premium-grid">@forelse($servers as $server)<article class="server-card"><div class="server-icon">{{ strtoupper(substr($server->game,0,2)) }}</div><div class="server-main"><small>{{ strtoupper($server->game) }}</small><h3>{{ $server->name }}</h3><p>{{ $server->host }}:{{ $server->port }} · {{ $server->current_map ?? 'Очаква query' }}</p></div><div class="server-live"><span class="status {{ $server->status }}">● {{ $server->status }}</span><b>{{ $server->players_online }}<small>/{{ $server->players_max }}</small></b></div></article>@empty<div class="feature-empty server-empty"><span class="empty-radar">⌁</span><b>Мрежата очаква първия сървър</b><p>Добави CS2, CS 1.6 или Minecraft сървър от контролния панел. NEXUS ще поеме live query статуса.</p>@auth @if(auth()->user()->isAdmin())<a class="btn btn-primary" href="{{ route('admin.servers.create') }}">Добави сървър →</a>@endif @else<a class="btn btn-primary" href="{{ route('login') }}">Администраторски вход →</a>@endauth</div>@endforelse</div>{{ $servers->links() }}</section></x-layouts.app>
