<x-layouts.app title="Сървъри · NEXUS"><section class="portal-subhead"><div class="portal-shell"><div class="portal-breadcrumb"><a href="{{ route('home') }}">HOME</a><span>/</span><b>SERVERS</b></div><small>LIVE NETWORK</small><h1>GAME SERVERS</h1><p>Live status, карти и играчи от цялата NEXUS мрежа.</p></div></section><div class="portal-shell portal-server-page"><aside class="server-filters"><b>SERVER BROWSER</b><a class="active" href="#">ALL SERVERS <span>{{ $servers->total() }}</span></a><a href="#">COUNTER-STRIKE 2</a><a href="#">CS 1.6</a><a href="#">MINECRAFT</a><div><small>QUERY ENGINE</small><strong><i></i> OPERATIONAL</strong><span>Refresh interval: 60 sec</span></div></aside><main><header class="portal-heading"><div><small>SERVER DIRECTORY</small><h2>ACTIVE NETWORK</h2></div><b>{{ $servers->sum('players_online') }} PLAYERS ONLINE</b></header><div class="server-directory"><div class="server-directory-head"><span>GAME</span><span>SERVER / ADDRESS</span><span>CURRENT MAP</span><span>PLAYERS</span><span>STATUS</span></div>@forelse($servers as $server)<article><span class="directory-game">{{ strtoupper(substr($server->game,0,2)) }}</span><div><b>{{ $server->name }}</b><code>{{ $server->host }}:{{ $server->port }}</code></div><div><b>{{ $server->current_map ?? 'Awaiting query' }}</b><small>{{ strtoupper($server->game) }}</small></div><div class="directory-players"><b>{{ $server->players_online }}<small>/{{ $server->players_max }}</small></b><em><i style="width:{{ $server->players_max ? min(100,($server->players_online/$server->players_max)*100):0 }}%"></i></em></div><span class="directory-status {{ $server->status }}">{{ strtoupper($server->status) }}</span></article>@empty<div class="portal-empty"><b>NO SERVERS CONFIGURED</b><p>Добави първия CS2, CS 1.6 или Minecraft сървър от контролния панел.</p>@auth @if(auth()->user()->isAdmin())<a href="{{ route('admin.servers.create') }}">ADD SERVER →</a>@endif @else<a href="{{ route('login') }}">ADMIN LOGIN →</a>@endauth</div>@endforelse</div><div class="server-pages">{{ $servers->links() }}</div></main></div></x-layouts.app>
