<x-layouts.app title="Сървъри · NEXUS"><section class="page-hero shell"><div class="eyebrow"><i></i> SERVER BROWSER</div><h1>Игрови сървъри</h1><p>Открий активните общности в NEXUS мрежата.</p></section><section class="shell page-body"><div class="server-grid">@forelse($servers as $server)<article class="server-card"><div class="server-icon">{{ strtoupper(substr($server->game,0,2)) }}</div><div class="server-main"><small>{{ strtoupper($server->game) }}</small><h3>{{ $server->name }}</h3><p>{{ $server->host }}:{{ $server->port }} · {{ $server->current_map ?? '—' }}</p></div><div class="server-live"><span class="status {{ $server->status }}">{{ $server->status }}</span><b>{{ $server->players_online }}<small>/{{ $server->players_max }}</small></b></div></article>@empty<div class="empty-state"><h3>Няма добавени сървъри</h3><p>Администраторът може да добави първия от контролния център.</p></div>@endforelse</div>{{ $servers->links() }}</section></x-layouts.app>

