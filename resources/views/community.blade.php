<x-layouts.app title="{{ app()->getLocale()==='bg'?'Общност':'Community' }} · NEXUS"><section class="n3-page-hero"><div class="n3-wrap"><div class="n3-breadcrumb"><a href="{{ route('home') }}">Home</a><span>/</span><b>Community</b></div><span class="n3-eyebrow">Member network</span><h1>{{ app()->getLocale()==='bg'?'Нашата общност':'Our community' }}</h1><p>{{ app()->getLocale()==='bg'?'Играчите, които изграждат NEXUS мрежата.':'The players who build the NEXUS network.' }}</p></div></section><div class="n3-content"><div class="n3-wrap"><div class="n3-heading"><div><span class="n3-eyebrow">Member directory</span><h2>{{ app()->getLocale()==='bg'?'Последни членове':'Latest members' }}</h2></div><strong class="text-[9px] text-slate-500">{{ number_format($memberCount) }} REGISTERED</strong></div><section class="n3-directory">@forelse($members as $member)<a class="n3-member" href="{{ route('profile.show',$member) }}">@if($member->avatar_url)<img src="{{ $member->avatar_url }}" alt="">@else<span>{{ mb_substr($member->name,0,1) }}</span>@endif<div><b>{{ $member->name }}</b><small>{{ $member->steam_id?'Steam verified':'Nexus member' }}</small></div><time>{{ $member->created_at->format('d.m.Y') }}</time></a>@empty<div class="n3-panel col-span-full p-14 text-center text-xs text-slate-500">No members yet.</div>@endforelse</section></div></div></x-layouts.app>
