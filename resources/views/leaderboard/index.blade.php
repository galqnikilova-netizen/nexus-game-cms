<x-layouts.app title="{{ app()->getLocale()==='bg'?'Класация':'Leaderboard' }} · NEXUS"><div class="n3-content"><div class="n3-wrap"><div class="n3-heading"><div><span class="n3-eyebrow">Player statistics</span><h1>{{ app()->getLocale()==='bg'?'Класация на играчите':'Player leaderboard' }}</h1></div><span class="n3-online"><i></i>{{ $stats->total() }} ranked players</span></div><div class="n3-page-grid"><aside class="n3-panel n3-filter">@auth<div class="n3-mini-user">@if(auth()->user()->avatar_url)<img src="{{ auth()->user()->avatar_url }}" alt="">@else<span>{{ mb_substr(auth()->user()->name,0,1) }}</span>@endif<div><b>{{ auth()->user()->name }}</b><small>{{ auth()->user()->steam_id?:'NEXUS MEMBER' }}</small></div><strong>{{ optional(auth()->user()->playerStats()->orderByDesc('score')->first())->score?:0 }}</strong></div>@endauth<form method="GET"><label class="n3-label">{{ app()->getLocale()==='bg'?'Избери сървър':'Select server' }}</label><select class="n3-input" name="server" onchange="this.form.submit()"><option value="">{{ app()->getLocale()==='bg'?'Всички сървъри':'All servers' }}</option>@foreach($servers as $server)<option value="{{ $server->id }}" @selected($serverId===$server->id)>{{ $server->name }}</option>@endforeach</select><span class="n3-label">{{ app()->getLocale()==='bg'?'Подреди по':'Sort by' }}</span><div class="n3-radios">@foreach(['score'=>'Игрови точки','kills'=>'Убийства','deaths'=>'Смърти','headshots'=>'Headshots','playtime_minutes'=>'Време в игра','last_seen_at'=>'Последна сесия'] as $key=>$label)<label><input type="radio" name="sort" value="{{ $key }}" @checked($sort===$key) onchange="this.form.submit()"><i></i>{{ $label }}</label>@endforeach</div><a class="n3-button ghost n3-full mt-4" href="{{ route('leaderboard.index') }}">Reset filter</a></form></aside><section class="n3-panel n3-table"><div class="n3-board-head"><span>#</span><span>Player</span><span>Rank</span><span>Kills</span><span>Deaths</span><span>K/D</span><span>HS</span><span>Playtime</span></div>@forelse($stats as $stat)<a class="n3-board-row" href="{{ route('profile.show',$stat->user) }}"><strong>{{ $stats->firstItem()+$loop->index }}</strong><span class="n3-player">@if($stat->user->avatar_url)<img src="{{ $stat->user->avatar_url }}" alt="">@else<i>{{ mb_substr($stat->user->name,0,1) }}</i>@endif<b>{{ $stat->user->name }}</b></span><span><em class="n3-rank">{{ $stat->score }}</em> {{ $stat->rank?:'Member' }}</span><span>{{ number_format($stat->kills) }}</span><span>{{ number_format($stat->deaths) }}</span><span>{{ number_format($stat->kd(),2) }}</span><span>{{ $stat->headshotRate() }}%</span><span>{{ intdiv($stat->playtime_minutes,60) }}h</span></a>@empty<div class="p-16 text-center text-xs text-slate-500">No player statistics yet.</div>@endforelse</section></div>@if($stats->hasPages())<div class="mt-5">{{ $stats->links() }}</div>@endif</div></div></x-layouts.app>
