<x-layouts.app title="Ban List · NEXUS"><section class="page-hero shell"><div class="eyebrow"><i></i> SECURITY NETWORK</div><h1>Ban list</h1><p>Прозрачен регистър на наказанията.</p></section><section class="shell page-body"><div class="ban-stats"><article><small>ОБЩО</small><b>{{ number_format($stats['total']) }}</b></article><article><small>АКТИВНИ</small><b>{{ number_format($stats['active']) }}</b></article><article><small>ПЕРМАНЕНТНИ</small><b>{{ number_format($stats['permanent']) }}</b></article></div><form class="ban-search"><input name="q" value="{{ $search }}" placeholder="SteamID, IP или име"><button class="btn btn-primary">Търси</button></form>@unless($enabled)<div class="empty-state"><h3>CSBans очаква връзка</h3><p>Добави CSBANS_DB_* настройките в .env.</p></div>@else<div class="ban-list">@forelse($bans as $ban)<article><div class="ban-state {{ $ban->is_active?'active':'expired' }}">{{ $ban->is_active?'ACTIVE':'EXPIRED' }}</div><div><small>PLAYER</small><h3>{{ $ban->player_nick ?: 'Unknown' }}</h3><code>{{ $ban->player_id ?: $ban->player_ip }}</code></div><div><small>ПРИЧИНА</small><b>{{ $ban->safe_reason ?: 'Не е посочена' }}</b><span>{{ $ban->server_name }}</span></div><div><small>АДМИН</small><b>{{ $ban->admin_nick ?: 'Console' }}</b><span>{{ $ban->created_at->format('d.m.Y H:i') }}</span></div><div><small>СРОК</small><b>{{ $ban->is_permanent?'Завинаги':$ban->expires_at->diffForHumans() }}</b></div></article>@empty<div class="empty-state"><h3>Няма намерени наказания</h3></div>@endforelse</div>{{ $bans->withQueryString()->links() }}@endunless</section></x-layouts.app>
