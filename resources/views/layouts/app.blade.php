<!doctype html>
<html lang="{{ str_replace('_','-',app()->getLocale()) }}">
<head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="csrf-token" content="{{ csrf_token() }}"><title>{{ $title ?? config('app.name','NEXUS') }}</title><meta name="description" content="NEXUS — modern game community platform"><meta name="theme-color" content="#070a08">@vite(['resources/css/app.css','resources/js/app.js'])</head>
<body>
<header class="site-header"><div class="shell nav-shell"><a class="brand" href="{{ route('home') }}" aria-label="NEXUS home"><span class="brand-mark">✣</span><span><b>NEXUS</b><small>GAME CMS</small></span></a><nav id="primary-navigation" aria-label="Primary navigation"><a class="{{ request()->routeIs('home')?'active':'' }}" href="{{ route('home') }}">Начало</a><a class="{{ request()->routeIs('servers.*')?'active':'' }}" href="{{ route('servers.index') }}">Сървъри</a><a class="{{ request()->routeIs('community.*')?'active':'' }}" href="{{ route('community.index') }}">Общност</a><a class="{{ request()->routeIs('shop.*')?'active':'' }}" href="{{ route('shop.index') }}">Магазин</a><a class="{{ request()->routeIs('bans.*')?'active':'' }}" href="{{ route('bans.index') }}">Ban list</a></nav><div class="nav-actions"><a class="locale-switch" href="{{ route('locale.switch',app()->getLocale()==='bg'?'en':'bg') }}">{{ app()->getLocale()==='bg'?'EN':'BG' }}</a>@auth<a class="user-chip" href="{{ route('profile.show',auth()->user()) }}">@if(auth()->user()->avatar_url)<img src="{{ auth()->user()->avatar_url }}" alt="">@else<span>{{ mb_substr(auth()->user()->name,0,1) }}</span>@endif</a>@if(auth()->user()->isAdmin())<a class="nav-admin" href="{{ route('admin.dashboard') }}">Панел</a>@endif @else<a class="nav-login" href="{{ route('login') }}">Вход</a>@endauth<button class="menu-toggle" type="button" aria-controls="primary-navigation" aria-expanded="false"><i></i><i></i><i></i><span class="sr-only">Меню</span></button></div></div></header>
<main>{{ $slot }}</main>
<footer><div class="shell footer-grid"><div><div class="brand"><span class="brand-mark">✣</span><span><b>NEXUS</b><small>BUILT FOR COMMUNITIES</small></span></div><p>Една платформа. Цялата ти gaming общност.</p></div><div><small>ПЛАТФОРМА</small><a href="{{ route('servers.index') }}">Сървъри</a><a href="{{ route('community.index') }}">Общност</a><a href="{{ route('shop.index') }}">Магазин</a></div><div><small>СИГУРНОСТ</small><a href="{{ route('bans.index') }}">Ban list</a><a href="{{ route('login') }}">Steam вход</a></div><span>© {{ date('Y') }} NEXUS</span></div></footer>
</body></html>
