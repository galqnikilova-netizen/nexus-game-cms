<!doctype html><html lang="{{ str_replace('_','-',app()->getLocale()) }}"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="csrf-token" content="{{ csrf_token() }}"><title>{{ $title ?? config('app.name','NEXUS') }}</title><meta name="theme-color" content="#070a08">@vite(['resources/css/app.css','resources/js/app.js'])</head><body><header class="site-header"><div class="shell nav-shell"><a class="brand" href="{{ route('home') }}"><span class="brand-mark">✣</span><span><b>NEXUS</b><small>GAME CMS</small></span></a><nav><a href="{{ route('servers.index') }}">{{ __('ui.nav.servers') }}</a><a href="#community">{{ __('ui.nav.community') }}</a><a href="#shop">{{ __('ui.nav.shop') }}</a></nav><div class="nav-actions"><a class="locale-switch" href="{{ route('locale.switch',app()->getLocale()==='bg'?'en':'bg') }}">{{ app()->getLocale()==='bg'?'EN':'BG' }}</a>@auth<a class="nav-admin" href="{{ route('admin.dashboard') }}">{{ __('ui.nav.control') }}</a>@else<a class="nav-login" href="{{ route('login') }}">{{ __('ui.nav.login') }}</a>@endauth</div></div></header><main>{{ $slot }}</main><footer><div class="shell footer-inner"><div class="brand"><span class="brand-mark">✣</span><span><b>NEXUS</b><small>BUILT FOR COMMUNITIES</small></span></div><p>{{ __('ui.footer') }}</p></div></footer></body></html>

