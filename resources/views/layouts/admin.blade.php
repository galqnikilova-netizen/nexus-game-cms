<!doctype html><html lang="bg"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="csrf-token" content="{{ csrf_token() }}"><title>{{ $title ?? 'NEXUS Control' }}</title>@vite(['resources/css/app.css','resources/js/app.js'])</head><body class="admin-body"><aside class="sidebar"><a class="brand" href="{{ route('admin.dashboard') }}"><span class="brand-mark">✣</span><span><b>NEXUS</b><small>CONTROL CENTER</small></span></a><div class="side-label">ОСНОВНИ</div><nav><a class="{{ request()->routeIs('admin.dashboard')?'active':'' }}" href="{{ route('admin.dashboard') }}">⌂ <span>Табло</span></a><a class="{{ request()->routeIs('admin.servers.*')?'active':'' }}" href="{{ route('admin.servers.index') }}">◉ <span>Сървъри</span></a><a href="#">♙ <span>Потребители</span></a><a href="#">▣ <span>Модули</span></a><a href="#">◈ <span>Магазин</span></a></nav><div class="side-label">СИСТЕМА</div><nav><a href="#">⚙ <span>Настройки</span></a><a href="{{ route('home') }}">↗ <span>Към сайта</span></a></nav><form method="POST" action="{{ route('logout') }}">@csrf<button>⇥ <span>Изход</span></button></form></aside><main class="admin-main"><header class="admin-top"><div><small>NEXUS / CONTROL</small><h1>{{ $heading ?? 'Контролен център' }}</h1></div><div class="admin-user"><span>{{ strtoupper(substr(auth()->user()->name,0,1)) }}</span><div><b>{{ auth()->user()->name }}</b><small>{{ auth()->user()->role }}</small></div></div></header><div class="admin-content">@if(session('success'))<div class="flash">✓ {{ session('success') }}</div>@endif{{ $slot }}</div></main></body></html>

