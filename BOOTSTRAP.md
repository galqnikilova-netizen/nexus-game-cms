# NEXUS Game CMS — Sprint 1 bootstrap

This commit safely transfers the complete Laravel source through Git objects. The included GitHub Actions workflow validates the compressed archive, expands the project into the repository root, removes the temporary chunks, and creates the final source commit.
